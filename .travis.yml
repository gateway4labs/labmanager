language: python

env:
  - LAB_ENV=test

install:
  - "pip install -r requirements.txt --use-mirrors --quiet"
  - "pip install -r requirements-test.txt --use-mirrors --quiet"

before_script:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

script: "python tests/integration_tests.py"
